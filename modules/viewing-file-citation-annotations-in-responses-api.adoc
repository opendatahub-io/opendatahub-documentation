:_module-type: PROCEDURE

[id="viewing-file-citation-annotations-in-responses-api-output_{context}"]
= Viewing file citation annotations in Responses API output

[role="_abstract"]
When you query ingested content by using the `file_search` tool with the Responses API, Llama Stack returns OpenAI-compatible `file_citation` annotations. These annotations identify the source files used during retrieval.

.Prerequisites
* You have deployed a Llama Stack server.
* You have configured at least one inference model.
* You have created a vector store and ingested content into it.
* You can successfully execute a RAG query by using the `file_search` tool, as described in 
ifdef::upstream[]
link:{odhdocshome}/working-with-llama-stack/#querying-ingested-content-in-a-llama-model_rag[Querying ingested content in a Llama model].
endif::[]
ifndef::upstream[]
link:{rhoaidocshome}{default-format-url}/working_with_llama_stack/querying-ingested-content-in-a-llama-model_rag[Querying ingested content in a Llama model].
endif::[]
* You have access to a client environment, such as a Jupyter notebook or an OpenAI SDK client, that is correctly configured to send authenticated requests to the Llama Stack server.

[NOTE]
====
This procedure requires that content has already been ingested into a vector store. If no content is available, RAG queries return empty or non-contextual responses.
====

.Procedure
. In a Jupyter notebook cell or other configured client environment, run a RAG query by using the `file_search` tool.
+
[source,python]
----
response = client.responses.create(
    model=model_id,
    input=query,
    instructions=system_instructions,
    tools=[
        {
            "type": "file_search",
            "vector_store_ids": [vector_store_id],
        }
    ],
)
----

. Inspect the full response object rather than only the `output_text` property.
+
[source,python]
----
response.output
----

. Access the `annotations` array.
+
[source,python]
----
annotations = response.output[0].content[0].annotations
print(annotations)
----

. Review the `file_citation` annotation fields.
+
Example output:
+
[source,json]
----
[
  {
    "type": "file_citation",
    "file_id": "file-57610eaac6364459bfefae60377837b7",
    "filename": "redbankfinancial_about.pdf",
    "index": 139
  }
]
----

Each `file_citation` annotation includes the following fields:

* `file_id`: The identifier of the retrieved file.
* `filename`: The name of the source file.
* `index`: The character index in the generated text where the citation applies.

Multiple annotations can reference the same index position.

.Optional: Using the OpenAI-compatible HTTP endpoint

If you use raw HTTP requests or an OpenAI SDK, send requests to the following endpoint:

`/v1/responses`

Ensure that your base URL includes the `/v1` path suffix, as described in 
ifdef::upstream[]
link:{odhdocshome}/working-with-llama-stack/#openai-compatibility-for-rag-apis-in-llama-stack_rag[OpenAI compatibility for RAG APIs in Llama Stack].
endif::[]
ifndef::upstream[]
link:{rhoaidocshome}{default-format-url}/working_with_llama_stack/openai-compatibility-for-rag-apis-in-llama-stack_rag[OpenAI compatibility for RAG APIs in Llama Stack].
endif::[]

.Verification

* The response includes an `annotations` array under `output[].content[]`.
* Each annotation has `"type": "file_citation"`.
* The `file_id` and `filename` correspond to files stored in the specified vector store.

