:_module-type: REFERENCE

[id='ref-grafana-metrics_{context}']
= Grafana metrics

[role='_abstract']

You can use Grafana boards to monitor the following metrics. The `datasource`, `instance` and `gpu` are variables defined inside the board. Ensure that you have also configured a namespace and pod to host the dashboard.

include::ref-accelerator-metrics.adoc[leveloffset=+1]
include::ref-cpu-metrics.adoc[leveloffset=+1]
include::ref-vllm-metrics.adoc[leveloffset=+1]